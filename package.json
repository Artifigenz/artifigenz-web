{
  "name": "artifigenz",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:web": "turbo dev --filter=@artifigenz/web",
    "dev:mobile": "cd apps/mobile && EXPO_NO_METRO_WORKSPACE_ROOT=1 npx expo start",
    "build:web": "turbo build --filter=@artifigenz/web"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
